#include<iostream>
#include<string>
#include<vector>
#include<math.h>


using ll = long long;


typedef struct{
    int seq_len = 1024; // seq_len
    ll vocab_size =  50257; // vocab size
    int n_layer = 12; // number of layers
    int n_head = 12; // number of head
    int n_embd = 768; // embedding dimension
}GPTconfig;












int main(){

    return 0;
}